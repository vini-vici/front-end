import React from 'react';
import { render, screen } from '@testing-library/react';
import TodoComponent from './todo.component';

test('Testing Rough Rendering', async () => {
  expect(1).toBe(1);
});